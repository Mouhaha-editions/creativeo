{% extends 'base.html.twig' %}

{% block title %}Edit Recipe{% endblock %}

{% block contentHeader %}
    {% include 'tools/content_header.html.twig' with {title: "title.recipe.edit"|trans } %}

{% endblock %}
{% block content %}
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">{{ "title.recipe.edit"|trans }}</h3>
        </div>
        <div class="card-body p-0">
            {{ include('recipe/_form.html.twig', {'button_label': 'button.default.edit'|trans }) }}
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('form_recipe') }}
{% endblock %}